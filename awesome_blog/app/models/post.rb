module WordCounter
    def word_count(num)
        puts num =  body.length
        if body.length = nil
            0
        end
    end
end

class Post < ApplicationRecord
    include WordCounter
end
